import React from 'react';
import { Box, Typography } from '@mui/material';

export const DataMapper: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Data Mapper</Typography>
      {/* Add your data mapper page content */}
    </Box>
  );
};

export default DataMapper; 